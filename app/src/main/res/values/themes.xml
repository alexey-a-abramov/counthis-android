<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.CountThis" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <item name="colorPrimary">@color/header_bg</item>
        <item name="colorPrimaryVariant">#0D1547</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/answer_a</item>
        <item name="colorSecondaryVariant">@color/answer_a</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:colorBackground">@color/settings_background</item>
        <item name="preferenceTheme">@style/PreferenceTheme.CountThis</item>
    </style>

    <!-- Custom Preference Theme with proper contrast -->
    <style name="PreferenceTheme.CountThis" parent="PreferenceThemeOverlay.v14.Material">
        <!-- Preference titles - high contrast -->
        <item name="android:textColorPrimary">@color/text_primary</item>

        <!-- Preference summaries - medium contrast, still readable -->
        <item name="android:textColorSecondary">@color/text_secondary</item>

        <!-- Category titles - accent color for hierarchy -->
        <item name="colorAccent">@color/purple_500</item>

        <!-- Preference background -->
        <item name="android:background">@color/settings_background</item>
    </style>
</resources>
